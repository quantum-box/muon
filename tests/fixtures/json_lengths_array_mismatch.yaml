name: JSON length array mismatch scenario
description: Expect failure when array length differs from expectation

config:
  base_url: __BASE_URL__

steps:
  - name: 配列の長さミスマッチ検証
    request:
      method: GET
      url: /sample
    expect:
      status: 200
      json_lengths:
        data.items: 3
